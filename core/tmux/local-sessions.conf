# Bounded local tmux navigation shared by every profile that enables it.
set -g detach-on-destroy off
bind-key T display-popup -E -w 80% -h 70% "$HOME/.local/share/wsl-plus/current/bin/wsl-plus session"
bind-key L switch-client -l
